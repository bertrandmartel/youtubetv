<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.bmartel.youtubetv.example">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/youtube"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Leanback">

        <activity
            android:name="fr.bmartel.youtubetv.example.YoutubeTvActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".samples.YoutubeActivityApiShowcase"
            android:label="@string/app_name" />

        <activity
            android:name=".samples.YoutubeActivityFragment"
            android:label="@string/app_name" />

        <activity
            android:name=".samples.YoutubeTvViewFullScreen"
            android:label="@string/app_name" />

        <activity
            android:name=".samples.YoutubeTvViewSplitted"
            android:label="@string/app_name" />

        <activity
            android:name=".samples.YoutubeTvViewDebug"
            android:label="@string/app_name" />

    </application>

</manifest>
